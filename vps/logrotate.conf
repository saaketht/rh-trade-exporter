# Update the path below to match your repo location, then:
#   sudo cp vps/logrotate.conf /etc/logrotate.d/rh-trade-exporter
/path/to/rh-trade-exporter/cron.log {
    weekly
    rotate 4
    compress
    missingok
    notifempty
}
